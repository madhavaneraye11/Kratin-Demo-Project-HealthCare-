<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.spam.maddy.kratindemoproject">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.providers.gsf.permisson.READ_GSERVICES" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoap"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logoap"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MedicalBookActivity"></activity>
        <activity android:name=".MedicalActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".BookActivity" />
        <activity android:name=".NetworkAct" />
        <activity android:name=".CovidActivity" />
        <activity android:name=".EmrgencyActivity" />
        <activity android:name=".PickExerciseActivity" />
        <activity android:name=".PickDishActivity" />
        <activity android:name=".MyMenuActivity" />
        <activity android:name=".ExerciseEditorActivity" />
        <activity android:name=".ExerciseActivity" />
        <activity android:name=".DishesEditorActivity" />
        <activity android:name=".DishActivity" />
        <activity android:name=".CalProfileActivity" />
        <activity android:name=".CalCalculatorActivity" />
        <activity android:name=".AlarmNotification" />
        <activity android:name=".AddDrAlarm" />
        <activity android:name=".DoctorReminderActivity" />
        <activity android:name=".MedAlarmActivity" />
        <activity android:name=".MedicineRemActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".NearByGymActivity"
            android:label="@string/title_activity_near_by_gym" />
        <activity
            android:name=".HospitalMapsActivity"
            android:label="@string/title_activity_hospital_maps" />
        <activity android:name=".HospitalActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".DOBActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".AlarmReceiver"
            android:enabled="true">
            <intent-filter android:priority="1">
                <action android:name="com.spam.maddy.kratindemoproject.BroadcastReceiver" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".AlarmReceiver1"
            android:process=":remote" />
        <receiver android:name=".BootCompletedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".MedRemInfo"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".AddReminder"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity
            android:name=".AlarmActivity"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert" />
    </application>

</manifest>